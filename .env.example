# ──────────────────────────────────────────────────────────────
# 网球记录助手 · 环境变量示例
# 本地开发：复制本文件为 .env 后填入真实值（.env 已被 git 忽略）
# 线上部署：在 Vercel 项目的 Settings → Environment Variables 里逐条添加
# ──────────────────────────────────────────────────────────────

# 数据存储驱动：supabase | pg | json
# 不填时自动判断（有 Supabase 配置就用 supabase，否则用本地 JSON 文件）
DATA_STORE=supabase

# Supabase 项目地址：控制台 → Project Settings → Data API → Project URL
SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase 服务端密钥：控制台 → Project Settings → API keys → service_role / secret
# ⚠️ 该密钥拥有整库读写权限，只能放在服务端，绝不提交到仓库或暴露给浏览器
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# 本地 API 端口（默认 3000）
API_PORT=3000

# 跨域白名单（只有前后端不同域时才需要，逗号分隔）
# CORS_ORIGINS=https://your-domain.com
