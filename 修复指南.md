# 🔧 错误修复指南

## ✅ 已修复的问题

问题：`border-border` 类不存在

**原因**：在 `src/index.css` 中错误地使用了 `@apply border-border;`

**已修复**：已将这行代码替换为标准的 CSS：
```css
* {
  box-sizing: border-box;
}
```

## 🔄 现在需要做什么

### 1. 重启开发服务器

在运行开发服务器的命令行窗口中：
- 按 `Ctrl + C` 停止服务器
- 重新运行：`npm run dev` 或双击 `start.bat`

### 2. 如果还有错误，确保所有依赖都已安装

在项目目录下运行：
```cmd
npm install
```

如果提示缺少 `tailwindcss-animate`，运行：
```cmd
npm install tailwindcss-animate
```

### 3. 清理缓存（如果还有问题）

有时需要清理缓存：

```cmd
# 删除 node_modules 文件夹
rmdir /s /q node_modules

# 删除 package-lock.json
del package-lock.json

# 重新安装依赖
npm install

# 启动服务器
npm run dev
```

## 📋 完整的修复步骤

### 方法 1：简单重启（推荐尝试）

1. 在命令行窗口按 `Ctrl + C` 停止服务器
2. 重新运行：`npm run dev`
3. 打开浏览器访问：http://localhost:5173

### 方法 2：重新安装依赖

如果方法 1 不行，执行以下步骤：

```cmd
# 1. 停止服务器（如果正在运行）
Ctrl + C

# 2. 重新安装所有依赖
npm install

# 3. 启动服务器
npm run dev

# 4. 打开浏览器
http://localhost:5173
```

### 方法 3：完全清理重装

如果前两种方法都不行，执行完整清理：

```cmd
# 1. 停止服务器
Ctrl + C

# 2. 删除 node_modules
rmdir /s /q node_modules

# 3. 删除缓存（如果有）
rmdir /s /q .vite

# 4. 重新安装
npm install

# 5. 启动服务器
npm run dev

# 6. 打开浏览器
http://localhost:5173
```

## 🔍 检查是否成功

成功启动后，命令行窗口应该显示：

```
VITE v5.4.10  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: http://192.168.xxx.xxx:5173/
```

然后在浏览器中访问 `http://localhost:5173`，应该能看到网球记录助手的首页。

## ⚠️ 如果还是有问题

### 检查 1：Node.js 版本

确保 Node.js 版本 >= 18：
```cmd
node --version
```

如果不是 18 或更高版本，请从 https://nodejs.org/ 下载最新 LTS 版本。

### 检查 2：npm 版本

确保 npm 版本 >= 9：
```cmd
npm --version
```

### 检查 3：依赖完整性

查看 `package.json` 确保所有依赖都存在。

### 检查 4：浏览器控制台

如果页面能打开但显示不正常，按 `F12` 打开浏览器开发者工具，查看 Console 标签的错误信息。

## 📞 获取帮助

如果问题仍然存在，请提供：

1. 命令行窗口的完整错误信息
2. 浏览器控制台的错误信息（按 F12 查看）
3. Node.js 和 npm 的版本信息

---

**提示**：大部分情况下，重启开发服务器就能解决问题！🎾
