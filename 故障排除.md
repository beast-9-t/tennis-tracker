# 🚨 故障排除指南

## 问题：服务器已启动但前端页面无法显示

### ✅ 已修复的错误

**错误信息**：
```
The `border-border` class does not exist.
```

**修复内容**：
- 文件：`src/index.css` 第 26 行
- 修改前：`@apply border-border;`
- 修改后：`box-sizing: border-box;`

---

## 🔧 立即解决方法

### 方法 1：重启服务器（最快）

1. **在命令行窗口中**，按 `Ctrl + C` 停止当前服务器
2. **重新运行**：`npm run dev`
3. **打开浏览器**：http://localhost:5173

### 方法 2：使用重启脚本（推荐）

在项目文件夹中，双击运行：
```
restart.bat
```

这个脚本会自动：
- 停止所有 Node.js 进程
- 清理 `.vite` 缓存
- 重新启动开发服务器

### 方法 3：完全重装（如果前两种方法不行）

1. 停止服务器：`Ctrl + C`
2. 删除依赖和缓存：
   ```cmd
   rmdir /s /q node_modules
   rmdir /s /q .vite
   del package-lock.json
   ```
3. 重新安装：
   ```cmd
   npm install
   ```
4. 启动服务器：
   ```cmd
   npm run dev
   ```

---

## 📋 分步操作指南

### Step 1: 确认服务器已停止

在命令行窗口中：
- 如果看到光标在闪烁，说明服务器正在运行
- 按 `Ctrl + C` 停止
- 等待几秒钟，确保完全停止

### Step 2: 检查端口是否被占用

如果提示端口被占用，可以：

**查看占用端口的进程**：
```cmd
netstat -ano | findstr :5173
```

**强制结束进程**（如果需要）：
```cmd
taskkill /F /PID <进程ID>
```

### Step 3: 重新启动

运行以下命令之一：
```cmd
npm run dev
```

或者双击运行：
```
start.bat
```

或
```
restart.bat
```

### Step 4: 等待启动完成

看到以下信息表示启动成功：
```
VITE v5.4.10  ready in 500 ms

➜  Local:   http://localhost:5173/
➜  Network: http://192.168.xxx.xxx:5173/
```

### Step 5: 访问网站

在浏览器中访问：
```
http://localhost:5173
```

---

## 🔍 常见问题诊断

### 问题 1：页面空白或 404 错误

**原因**：
- 服务器未完全启动
- 浏览器缓存问题

**解决方法**：
1. 确保命令行显示 `ready in xxx ms`
2. 清除浏览器缓存：`Ctrl + Shift + R`
3. 尝试使用无痕模式打开

### 问题 2：样式显示不正常

**原因**：
- Tailwind CSS 未正确加载
- 缓存问题

**解决方法**：
1. 强制刷新：`Ctrl + Shift + R`
2. 清除浏览器缓存
3. 重启服务器

### 问题 3：浏览器控制台有错误

**解决方法**：
1. 按 `F12` 打开开发者工具
2. 查看 Console 标签
3. 查看具体错误信息
4. 参考 [修复指南.md](./修复指南.md)

### 问题 4：修改代码后页面不更新

**解决方法**：
1. Vite 应该自动热更新（HMR）
2. 如果不更新，刷新页面：`F5`
3. 或强制刷新：`Ctrl + Shift + R`

---

## ✅ 成功标志

服务器启动成功后，您应该看到：

### 命令行窗口显示：
```
  VITE v5.4.10  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.1.100:5173/
  ➜  press h + enter to show help
```

### 浏览器显示：
- 🎾 网球记录助手标题
- 📊 统计卡片（总场次、总时长、本周场次）
- 📅 最近一场比赛
- 🎯 本周目标进度
- 📝 最近记录列表
- 🧭 底部导航栏
- ➕ 右下角添加按钮

---

## 📞 获取更多帮助

### 查看文档

- [修复指南.md](./修复指南.md) - 详细的修复步骤
- [启动指南.md](./启动指南.md) - 如何启动项目
- [SETUP.md](./SETUP.md) - 安装和配置指南

### 检查环境

确认开发环境正确：

**检查 Node.js 版本**：
```cmd
node --version
```
需要 >= 18

**检查 npm 版本**：
```cmd
npm --version
```
需要 >= 9

---

## 💡 快速参考

### 常用命令

```cmd
# 启动服务器
npm run dev

# 停止服务器
Ctrl + C

# 重启服务器
restart.bat

# 安装依赖
npm install

# 清理缓存
rmdir /s /q .vite
```

### 常用快捷键

| 操作 | 快捷键 |
|------|--------|
| 停止服务器 | `Ctrl + C` |
| 刷新页面 | `F5` |
| 强制刷新 | `Ctrl + Shift + R` |
| 打开开发者工具 | `F12` |

---

**现在就试试重启服务器吧！** 🚀

1. 按 `Ctrl + C` 停止服务器
2. 运行 `npm run dev` 或双击 `restart.bat`
3. 访问 http://localhost:5173

祝您使用愉快！🎾
