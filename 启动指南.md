# 🚀 如何启动网球记录助手网站

## 方法 1：使用文件资源管理器（最简单）

### 1. 打开项目文件夹
- 打开 Windows 文件资源管理器（快捷键 Win + E）
- 在地址栏输入：`c:\Users\o\CodeBuddy\20260209102439\tennis-tracker`
- 按回车键

### 2. 启动开发服务器
在文件夹中找到并双击运行：
- `start.bat` - 这个文件会自动启动开发服务器

或者按顺序双击：
- `install.bat` - 安装依赖（只需第一次运行）
- `run.bat` - 运行开发服务器

### 3. 打开浏览器
等待几秒钟后，打开浏览器访问：
```
http://localhost:5173
```

## 方法 2：使用命令提示符

### 1. 打开命令提示符
- 按 Win + R，输入 `cmd`，按回车

### 2. 进入项目目录
复制并粘贴以下命令：
```cmd
cd /d c:\Users\o\CodeBuddy\20260209102439\tennis-tracker
```

### 3. 安装依赖（只需第一次）
```cmd
npm install
```

### 4. 启动开发服务器
```cmd
npm run dev
```

### 5. 复制地址
命令窗口会显示类似这样的信息：
```
VITE v5.4.10  ready in 500 ms

➜  Local:   http://localhost:5173/
➜  Network: http://192.168.1.100:5173/
```

复制 `http://localhost:5173/` 到浏览器地址栏打开

## 方法 3：使用 PowerShell

### 1. 打开 PowerShell
- 按 Win + R，输入 `powershell`，按回车

### 2. 进入项目目录
```powershell
Set-Location -Path "c:\Users\o\CodeBuddy\20260209102439\tennis-tracker"
```

### 3. 安装依赖（只需第一次）
```powershell
npm install
```

### 4. 启动开发服务器
```powershell
npm run dev
```

## 🔍 如何找到项目文件夹

### 方式 1：通过地址栏直接访问
1. 打开任意文件夹窗口
2. 点击地址栏
3. 输入：`c:\Users\o\CodeBuddy\20260209102439\tennis-tracker`
4. 按回车

### 方式 2：通过 CodeBuddy 工作区
1. 在您的代码编辑器中（如 VS Code）
2. 打开文件夹：`c:\Users\o\CodeBuddy\20260209102439`
3. 找到 `tennis-tracker` 子文件夹

### 方式 3：使用运行对话框
1. 按 Win + R
2. 输入：`c:\Users\o\CodeBuddy\20260209102439\tennis-tracker`
3. 按回车

## 📁 项目文件夹结构

```
tennis-tracker/
├── src/               # 源代码
├── public/            # 静态资源
├── start.bat          # 启动脚本（推荐）
├── run.bat            # 运行脚本
├── install.bat        # 安装脚本
├── package.json       # 项目配置
└── index.html         # HTML 入口
```

## ⚠️ 常见问题

### Q: 提示找不到 npm 命令？
A: 需要先安装 Node.js：https://nodejs.org/

### Q: 端口 5173 被占用？
A: 关闭占用该端口的程序，或修改 `vite.config.ts` 中的端口配置

### Q: 浏览器无法访问？
A: 确保开发服务器已启动，窗口显示 `ready in xxx ms`

### Q: 样式显示不正常？
A: 确认已安装所有依赖，尝试删除 `node_modules` 文件夹后重新安装

## 💡 推荐方法

**最简单的方式**：
1. 打开文件资源管理器
2. 访问：`c:\Users\o\CodeBuddy\20260209102439\tennis-tracker`
3. 双击 `start.bat`
4. 等待几秒
5. 打开浏览器访问：http://localhost:5173

就这么简单！🎾
